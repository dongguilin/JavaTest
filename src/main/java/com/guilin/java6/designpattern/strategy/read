策略模式
Strategy pattern或者Policy pattern(政策模式)
优点：
    算法可以自由切换
    避免使用多重条件判断
    扩展性良好
缺点：
    策略类数量增多
    所有策略类都需要对外暴露(可以使用工厂模式、代理模式、享元模式等修正这个缺陷)
使用场景
    多个类只有在算法或行为上稍有不同的场景
    算法需要自由切换的场景
    需要屏蔽算法规则的场景